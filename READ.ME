## Tested on  
- `Python 3.7.3`

## Set up Environment  (One time only)

1. Install pipenv using `pip install pipenv`
2. Go to virtual shell using `pipenv shell`
3. Install dependancies `pipenv install`

## Run Application
- Run with command `flask run`
